import colors from "./Colors";

const toastStyle = {
    style: { background: colors.primary, color: colors.secondary, borderRadius: '8px' },
    iconTheme: { primary: colors.primary, secondary: colors.secondary },
}
export default toastStyle;